services:
  postgres:
    image: ankane/pgvector:latest
    environment:
      POSTGRES_USER: knova
      POSTGRES_PASSWORD: knova_secret
      POSTGRES_DB: knova
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U knova"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@knova.dev
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy

  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  backend:
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      FRONTEND_URL: http://localhost:3000

      DATABASE_URL: postgresql://knova:knova_secret@postgres:5432/knova

      JWT_SECRET: change-me-in-production-min-32-chars
      JWT_REFRESH_SECRET: change-me-refresh-in-production-min-32
      JWT_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 30d

      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ""

      LLM_PROVIDER: centrinvest
      CENTRINVEST_API_KEY: hackaton2026
      CENTRINVEST_LLM_URL: https://hackai.centrinvest.ru:6630/v1
      CENTRINVEST_EMBED_MODEL: Qwen3-Embedding-0.6B
      CENTRINVEST_EMBEDDING_URL: https://hackai.centrinvest.ru:6620/v1
      CENTRINVEST_STT_URL: https://hackai.centrinvest.ru:6640/v1
      CENTRINVEST_STT_MODEL: whisper-large-v3-turbo

      GEMINI_API_KEY: ""
      GEMINI_MODEL: gemini-2.0-flash

      OLLAMA_BASE_URL: http://ollama:11434
      OLLAMA_CHAT_MODEL: llama3.2
      OLLAMA_EMBED_MODEL: nomic-embed-text

      EMBEDDING_DIMENSIONS: 1024

      UPLOAD_DIR: /app/uploads
      MAX_FILE_SIZE: 104857600

      BULL_BOARD_USERNAME: admin
      BULL_BOARD_PASSWORD: admin

      ML_SERVICE_URL: http://ml:8000
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./apps/frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://localhost:3001
        NEXT_PUBLIC_WS_URL: http://localhost:3001
        NEXT_PUBLIC_APP_NAME: Knova
        NEXT_PUBLIC_APP_VERSION: "1.0.0"
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
  ollama_data:
  uploads_data:
